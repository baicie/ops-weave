import { ModulePlaceholder } from '../../components/ModulePlaceholder.tsx'

export function SkillsPage() {
  return (
    <ModulePlaceholder
      title="Skill"
      capability="Skill 发布、不可变版本和测试集由 Java ai-control 拥有，当前没有 UI。"
      next="不要在浏览器加载任意用户路径；Runtime 只执行已授权的固定版本包。"
    />
  )
}
