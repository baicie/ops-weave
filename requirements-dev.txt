# Optional repository checks only. No deployed service requires Python.
pytest>=8,<10
jsonschema>=4.23,<5
# jsonschema otherwise silently skips date-time format validation.
rfc3339-validator==0.1.4
PyYAML>=6.0.2,<7
