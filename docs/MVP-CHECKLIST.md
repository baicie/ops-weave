# 只读诊断 MVP 目标

目标状态：**进行中**（2026-09-25 用户明确要求推进到 100%）。在当前 `main` 工作区推进，不新建分支。范围与退出条件来自 ROADMAP M0–M4；不把 M5 可恢复 Agent、Copilot 或生产写入动作提前纳入。

完成必须有实际验收证据。下面的“本机通过”不等于真实来源和真实模型验收；不按测试条数折算百分比。

| 阶段 | 必须交付并验证 | 当前状态 |
|---|---|---|
| M0 | 锁文件、Wrapper、Zeus 基线与构建 | 已关闭；历史证据见验证报告 |
| M1 | 可信身份、租户/对象权限、确定的 401/403/404、会话失效和旧响应清理；资源筛选/时间范围恢复 | 开发 Principal/对象授权、请求/开发凭据和 HTTP 边界本机通过（第 31 节）；资产/Incident/指标只读选择与实际窗口恢复、无自动请求/逐次授权通过（第 35 节）；OIDC BFF/PKCE/Cookie/CSRF、服务端撤权/有界 Runtime 委托/双标签退出本机通过（第 36 节）；Worker 独立服务 JWT/运维有界授权、撤销/故障不推进游标及密钥轮换续采本机通过（第 37 节）；真实 IdP/TLS/代理仍缺 |
| M2 | 支持版本明确的真实 Zabbix Host → 已发布映射 → Raw/Observation/Entity；重复同步和失败不误删除 | 本机协议桩/fixture、PG、Preview/版本/草稿/持久只读重放通过；第41节补齐Host持久来源租约、过期接管、旧扫描写入/对账隔离和等待锁过期回滚；第45节让 Item 扫描取得同一来源 scope 租约并把目录/绑定写入与缺失对账放进围栏；第46节让 Host 走 hostid 水位快照（计数+水位双验证，漂移按 `SOURCE_SCAN_UNVERIFIED` 拒绝对账）；第47节把边界标签持久化并在追溯页显示；第49节把“只有已验证快照才允许对账”变成用例层强制检查；第51节补齐只读来源连接自检（记录来源自报版本，作为“已声明支持版本”的输入）；第44节补齐已存储扫描运行的只读追溯（分页/单条、失败码与钉住版本，不含重试/对账）；真实实例未提供，水位快照假定 hostid 单调、自报版本未与真实实例核对 |
| M2 | 资产服务端筛选/分页/详情、来源/映射/观测时间、跨租户隔离和冲突追溯 | 筛选/分页/详情、观测历史、字段审核和 UUID 登记/撤销已有；第42节显式快照/来源确认/生命周期保护本机通过，第43节补齐新观测驱动的人工绑定更正、旧归属审计与页面。厂商API/后台采集、通用权威规则、已有实体合并/alias与真实厂商验收仍缺 |
| M3 | 指标目录/绑定、实际采集存储和查询，保留缺失/过期/来源 | 本机 PG/VM 与独立服务身份 Worker 链通过（第 37 节）；第45节给 Item 采集加来源 scope 租约与受围栏写入/对账，第48节让 itemid 水位双验证成为完成前提（漂移按 `SOURCE_SCAN_UNVERIFIED` 拒绝对账），第49节把该前置条件变成用例层强制检查；第52节补齐 SUM/计数器变化率与 reset 策略（下降区间按零重计并标注、原始点保留、Web 变化率视图与 reset 标记，非法响应拒绝），M3 本地可验证条件全部有证据；真实厂商采样与真实 Zabbix 行为仍待验收 |
| M3 | 外部告警/恢复幂等导入、Incident 状态/时间线/关联实体，人工合并拆分 | 原子导入/PG 状态/页面、人工合并拆分/后续归属/原子回执历史与旧证据失效，以及合并前不可变规范化告警历史本机通过（ADR-021/024/028、第 27/30/34 节）；真实来源验收仍缺，厂商原始报文不在本次留存范围 |
| M4 | Java 版本化 incident.get / metric.summary、受权 Evidence、审计和预算 | 三个 v2 Tool、当前知识会话、PG 证据/审计/原子预算、超时和范围复核本机通过；正文/读取审计策略清理、保留Incident、旧标识和授权元数据存续见第40节；真实身份/来源与全平台总量/备份生命周期仍缺 |
| M4 | Rust 真实平台 HTTP + 单次模型摘要；共享预算/截止时间；前后校验证据 | 当前知识结构化流程、新 Skill 2.0.0、单次模型/两次证据复核已通过本机 mock/Java/PG/VM 联调和失败负例（第 29 节）；模型预算/原费率/用量/未知费用与 Web 查询已通过本机协议桩和 PG 链（第39节）；第50节把请求边界钉成证据：密钥只在 `Authorization` 头且不出现在请求体、提问与知识以具名不可信字段随真实技能提示发出、失败无重试无回退且 provider 报错不落库（本地协议桩）；真实 Rig 调用及账单仍未验收，原历史 demo 未放宽 |
| M4 | AIInsight 幂等持久化及查询、页面发起/结果/受权证据 | Java 可信字段、Runtime 本机提交证明、PG 原子幂等保存、网页发起/刷新回读/证据重授权本机通过；24小时证据到期使结果不可读；V019显式正文清理保留原运行/权限/费用，Web预览/确认/原回执恢复见第40节。真实身份与全平台元数据/备份留存仍缺 |
| M4 | 一条真实来源、一个真实 Incident、一组真实指标完成诊断；异常负例全部通过且抽样人工审阅 | 尚未验收；真实来源/模型/登录配置和人工审阅不可由本机 fixture 替代 |
| 治理 | 运行/回执类表的增长必须有界，且不改变业务语义 | 第45节回执按 tenant/source 保留 100 份；第54节扫描运行记录按范围 1000 行/租户 5000 行预算，打开扫描时清理最旧的已结束运行，未结束与被映射版本钉住的运行永不删除，读取不清理。**其余业务/授权元数据仍无时间窗口或总量配额**，边界与顺序见 [ADR-048](adr/048-metadata-retention-backup-lifecycle.md)（设计，不是已交付能力） |

执行顺序：当前知识诊断、结果/证据、Incident 人工关联、公共请求/开发会话、资产观测追溯、固定补充来源字段审核、规范化告警历史和核心只读筛选 URL 恢复已完成本机切片；OIDC BFF/委托、Worker 服务身份、人工核对 UUID 登记与定位也已通过本机完整链。费用预留/Token 用量/未知费用查询已补齐（第39节），金额仍为配置估算；AI正文与读取审计留存已补齐（第40节）。第42节补齐固定来源的显式快照解析与存在确认，第43节补齐新观测驱动的人工绑定更正、历史归属保留与有界更正历史，第44节补齐已存储扫描运行的只读追溯，第45节让 Item 扫描取得来源 scope 租约并把写入与对账放进围栏，第46节让 Host 走 hostid 水位快照并在漂移时拒绝对账，第47节把该边界标签持久化并在追溯页显示，第48节让 Item 采集走 itemid 水位快照，第49节把“只有已验证快照才允许对账”变成用例层强制检查（Problem/历史采集经审计确认无缺失对账），第50节为模型请求边界补上密钥隔离与不可信数据框架的测试证据，第51节补齐只读来源连接自检，第52节补齐计数器变化率与 reset 策略（M3 此前唯一的本地缺失项），M3 约90%、MVP 约91%。继续来源冲突/失败追溯、只读诊断异常场景评估与运行记录治理（清理/配额）；厂商自动采集、已有实体合并/alias、其他业务/授权元数据总量和备份生命周期仍需治理。身份登记不等于已有实体合并；历史查询不等于完整来源事件日志或任意 asOf 投影；扫描追溯不等于重试、对账或修复；来源所有权不等于上游一致快照，水位快照也不等于厂商实例验收；本地协议桩的请求边界证据不等于真实提供方验收；连接自检记录的是来源自报版本，不等于支持声明已核对；变化率是平台按固定策略推导的视图，不等于厂商上报速率；URL 恢复不包含历史子面板、写操作或执行输入。真实环境配置一旦可用就穿插验证，不等待全部代码结束才联调。

当前环境依赖：2026-09-25 用户明确回复“暂未准备，先推进本地实现”。继续本地代码、显式 fixture/协议桩及真实 PG/VM 验证；真实 Zabbix、模型和登录提供方验收保留为未完成，不反复索要配置，也不宣称 MVP 已达 100%。后续准备好时只需配置文件路径或环境变量名称，不在任务消息/普通日志中传递密钥。2026-09-26 用户选择“本地治理收尾”作为当前推进方向，并要求推送 main（CI 四 job 全绿后由 deploy job 部署到选定主机）。

已知收尾项：指标选择器已迁移到有界 `/entities/page`；旧 `/entities` 兼容 API 已复用有界授权查询，超过 100 条明确返回 422，需迁移分页，不再静默全量扫描或截断。当前分页是 UUID 顺序的实时视图，不是跨页一致性快照。Incident 快照有 50 个问题/100 条时间线上限，暂未提供时间线分页。

进度估算见 [PROGRESS.md](PROGRESS.md)，实际执行记录见 [VALIDATION-REPORT.md](VALIDATION-REPORT.md)。
