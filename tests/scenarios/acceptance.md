# 下一批验收场景（尚未实现自动化）

- 两租户/两网络相同 IP，不误合并。
- 来源快照分页中断，不误删除。
- 同一个事件重复处理，不重复通知。
- 重放修复投影，不能触发 Action 或模型无限循环。
- user CPU 与 total CPU 语义不混；counter reset 不产生负速率。
- 历史 RCA 不能使用故障发生后的文档作为当时证据。
- 模型供应商失败不影响 Incident / 告警功能。
- 审批后参数或资源版本改变，执行被拒绝。
- 服务重启恢复任务，无副作用重复执行。
- 生产 OIDC、RLS、缓存和向量 ACL 隔离通过集成测试。
